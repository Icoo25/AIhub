import type { ReactNode } from "react";
export function PageHeading({ eyebrow, title, description, action }: { eyebrow: string; title: string; description: string; action?: ReactNode }) { return <div className="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-1.5 text-[9px] font-semibold uppercase tracking-[.14em] text-[#767869]">{eyebrow}</p><h1 className="text-3xl font-semibold tracking-[-.035em] text-[#1b1c16]">{title}</h1><p className="mt-1.5 max-w-xl text-[11px] leading-relaxed text-[#767869]">{description}</p></div>{action}</div>; }
